# Example frontend env (for Vite)
# Copy to `frontend/.env` or set in Vercel project settings

# API base URL for the backend (set in Vercel/Render for production)
VITE_API_URL=https://your-backend-url.example
